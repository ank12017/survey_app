<p id="notice"><%= notice %></p>

<h1>Surveys</h1>

<table  style="width:10%">
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @surveys.each do |survey| %>
      <tr>
      <td><%= survey.name %></td>
        <td><%= link_to 'Test', test_survey_path(survey) %></td>
        <td><%= link_to 'result', test_survey_path(survey) %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
